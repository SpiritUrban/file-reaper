# TrashRadar — Execution Progress

> Журнал виконання задач з [tasks.md](tasks.md). Одна задача = один рядок.
> Правила ведення: статус міняється лише разом з фактом (коміт/PR/верифікація);
> нова сесія починає з першого рядка без ✅ у порядку tasks.md.

## Стан середовища розробки (локальна машина)

- Node 20.19.6, npm 10.8.2
- Rust 1.96.1 stable-msvc (rustup) + rustfmt + clippy; MSVC Build Tools 2022; WebView2 — є
- Tauri CLI 2.11.4 — через `ui` devDependencies (`npm run tauri`)
- Запуск повного застосунку: з `core/`: `..\ui\node_modules\.bin\tauri.cmd dev --config shell/tauri.conf.json`

## Журнал задач

| Задача | Статус | Дата | Де | Нотатки |
|---|---|---|---|---|
| T-001 Каркас (Tauri+Rust+React) | ✅ | 2026-07-08 | PR #1 | 13 крейтів + UI; `cargo check/clippy/fmt` чисто; UI build чисто; 4 екрани перевірені у браузері. Іконка-заглушка `core/shell/icons/icon.ico` (tauri-build вимагає .ico) — фінальна у T-159 |
| T-002 CI | ✅ | 2026-07-08 | PR #2 | 3 джоби (UI / Core / Core shell+rust-cache) — зелені; installer.yml збирає NSIS-артефакт на push у main + dispatch; локальна збірка інсталятора верифікована (TrashRadar_0.1.0_x64-setup.exe, 1.9 МБ). Попутно виправлено tauri.conf: before*Command виконуються з кореня workspace (../ui, не ../../ui). Branch protection НЕ застосовано — див. відхилення. Автовидалення гілок після merge увімкнено |
| T-003 Логування Core | ✅ | 2026-07-08 | PR #3 | tracing + tracing-subscriber (env-filter); власний RotatingWriter за розміром (5 МБ, 3 бекапи, 4 юніт-тести); файл %LOCALAPPDATA%\TrashRadar\logs\core.log; рівні через TRASHRADAR_LOG; паніки логуються; збій init → деградація до stderr. Живий запуск верифікував стартовий запис у файлі |
| T-004 Канал команд UI→Core | ✅ | 2026-07-08 | PR #5 | shell/src/ipc.rs: модуль команд; app.ping {delayMs?, fail?} — постійна діагностична команда (roundtrip + неблокування + конверт помилок E2E). 4 інтеграційні тести через tauri mock runtime (tauri/test dev-фіча): roundtrip, помилка = конверт T-007, паралельність (швидка <300мс поки повільна спить 400мс), deny_unknown_fields. UI: handshake ping при старті. Живий запуск: ping від реального UI у core.log. +tokio/time (вже в дереві) |
| T-005 Канал подій Core→UI | ✅ | 2026-07-08 | PR #6 | shell/src/events.rs: реєстр топіків + emit() (fire-and-forget, з логуванням). Дротові імена: Tauri забороняє крапки в подіях → app.test ↔ app:test (wire_name ↔ client.ts). app.test_stream: ack одразу, події у фоні (модель architecture.md §1.2). Capabilities: core/shell/capabilities/default.json (core:default) — без нього webview не має права listen. 3 тести шини (доставка, unlisten зупиняє, ack до завершення потоку). UI self-check обох каналів при старті. Живий запуск: 3 емісії в core.log |
| T-006 Тротлінг подій | ✅ | 2026-07-08 | local | Додано CounterThrottle у shell/src/events.rs: агрегує дельти лічильників у CounterSnapshot {delta,total}, тримає темп ≤10 оновлень/с і flush без втрати підсумку. Діагностичний app.test_stream тепер паралельно шле throttled app.test_counter; контракт і TS EventName синхронізовані. Перевірено: cargo fmt --check; CARGO_INCREMENTAL=0 cargo clippy -p trashradar-shell -- -D warnings; CARGO_INCREMENTAL=0 cargo test -p trashradar-shell (13 passed); npm run build |
| T-007 Модель помилок Core | ✅ | 2026-07-08 | PR #4 | domain::error::CoreError: конверт {code, message}, 5 стартових стабільних кодів (internal/invalid_argument/not_implemented/io/cancelled), From<io::Error>, 4 тести форми/roundtrip. UI: CoreErrorPayload у types.ts, CoreIpcError + нормалізація в client.ts (+ клієнтські коди ipc_unavailable/unknown). Конверт зафіксовано в contracts/ipc-contract.json → error_envelope. E2E через живий IPC — вправляється у T-004 |
| T-009 Діагностичний екран health | ✅ | 2026-07-08 | local | Додано UI-екран Health з live polling app.health: версія Core, статус, модулі shell/ipc/scanner/index/quarantine та IPC-лічильники commands/events/errors. Core накопичує метрики команд і подій, контракт app.health розширено для T-009, TS типи синхронізовані. Перевірено: cargo fmt --check; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo clippy -p trashradar-shell -- -D warnings; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo test -p trashradar-shell (13 passed); npm run build |
| T-008 Пул фонових воркерів | ✅ | 2026-07-08 | local | Додано WorkerPool у core/app: пріоритетна BinaryHeap-черга, FIFO у межах одного пріоритету, JobHandle з cancel/wait, CancellationToken для кооперативної відміни між кроками. Покрито тестами: пріоритетна задача виконується перед background у черзі; queued job скасовується до старту; running job бачить cancel між кроками. Перевірено: cargo fmt --check; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo clippy -p trashradar-app -- -D warnings; cargo test -p trashradar-app (3 passed); cargo clippy -p trashradar-shell -- -D warnings; cargo test -p trashradar-shell (13 passed) |
| T-010 SQLite WAL-обгортка | ✅ | 2026-07-08 | local | У core/infra/index-sqlite додано IndexDatabase: відкриття БД у профілі як index.sqlite3, створення директорії профілю, PRAGMA journal_mode=WAL/synchronous=NORMAL/foreign_keys=ON/busy_timeout, bootstrap metadata-таблиця та типізована помилка адаптера. Покрито тестами: БД створюється у профілі з WAL; окремий reader читає останній committed snapshot під час активної writer-транзакції. Перевірено: cargo fmt --check; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo test -p trashradar-index-sqlite (2 passed); CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo clippy -p trashradar-index-sqlite -- -D warnings |
| T-011 Схема БД v1 | ✅ | 2026-07-08 | local | У core/infra/index-sqlite додано documented migration v1 через SCHEMA_V1_SQL і PRAGMA user_version=1. Схема file_records покриває CandidateId, шлях, розмір, created/modified/accessed FILETIME, тип файлу, одиницю file/folder, категорію MVP, safety/decision, detector_id/explanation та FS-прапорці raw/read-only/hidden/system/temporary; додано індекси для майбутніх category-window запитів. Додано FileRecord/FileAttributes та upsert/read roundtrip. Перевірено: cargo fmt --check; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo test -p trashradar-index-sqlite (4 passed); CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo clippy -p trashradar-index-sqlite -- -D warnings |
| T-012 Механізм міграцій схеми | ✅ | 2026-07-08 | local | У core/infra/index-sqlite додано версійний runner міграцій з LATEST_SCHEMA_VERSION/MIGRATIONS, транзакційне виконання SQL-міграцій і typed SchemaMigrationFailed. Відкриття БД тепер автоматично мігрує стару T-010 БД до v1; якщо міграція старої схеми падає, адаптер закриває connection, видаляє index.sqlite3 та WAL/SHM sidecar-файли і відкривається з чистою схемою v1 без крашу. Покрито тестами: міграція T-010 meta БД зі збереженням даних; cold start після конфліктної старої таблиці file_records. Перевірено: cargo fmt --check; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo test -p trashradar-index-sqlite (6 passed); CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo clippy -p trashradar-index-sqlite -- -D warnings |
| T-013 Батчевий запис через єдину чергу писальника | ✅ | 2026-07-08 | local | У core/infra/index-sqlite додано upsert_file_records_batch через один BEGIN IMMEDIATE transaction і спільний prepared UPSERT для одиночного та батчевого запису. Додано IndexWriterQueue/IndexWriter: один background writer thread відкриває IndexDatabase, приймає батчі через mpsc-чергу та серіалізує всі записи в SQLite, щоб паралельні producer-и не створювали writer contention. PRAGMA cache_size/wal_autocheckpoint налаштовано для довгих батчів. Покрито тестами: батчевий upsert roundtrip; 8 конкурентних producer-ів через одну writer-чергу без database locked; ignored perf-gate на 1 000 000 записів батчами по 100k, окремо запущений і пройдений за ~64с при локальній цілі <=70с. Перевірено: cargo fmt --check; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo test -p trashradar-index-sqlite (8 passed, 1 ignored); CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo test -p trashradar-index-sqlite writer_queue_inserts_one_million_records_in_batches -- --ignored --nocapture (1 passed); CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo clippy -p trashradar-index-sqlite -- -D warnings |
| T-014 Віконні (паговані) запити читання з сортуванням | ✅ | 2026-07-08 | local | `FileRecord`/`FileAttributes` перенесено до доменного крейту. Оголошено метод `read_file_records_window` в порті `IndexStore` та реалізовано в `IndexDatabase` з оптимізацією через відкладені з'єднання (deferred joins) для запобігання сканування всієї таблиці при великих зсувах (offsets). Додано unit-тести коректності та ігнорований perf-тест на 1 млн записів (виконується < 50мс з підігрітим кешем). Перевірено: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` (всі тести успішно пройшли). |
| T-015 Компактна структура запису: інтерновані шляхи, числові атрибути | ✅ | 2026-07-08 | local | У core/infra/index-memory додано структуру `CompactFileRecord` (48 байт, timestamps як u32 Unix Epoch seconds, enums упаковані в u16) та `PathInterner` (arena-подібний інтернер з єдиним рядковим буфером та зсувами offset). Додано метод `finish_indexing()`, який очищує HashMap lookup-таблиці та вивільняє оперативну пам'ять після наповнення індексу. Покрито тестами: interning/resolution; розмір `CompactFileRecord` (48 байт); roundtrip пакування/розпакування; і бенчмарк на 5 000 000 записів, який використовує ~266 МБ оперативної пам'яті (ціль < 300 МБ виконано). Перевірено: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` (всі тести пройшли). |
| T-016 Потокобезпечне наповнення індексу батчами від сканера | ✅ | 2026-07-08 | local | Оголошено трейт `HotIndex` у `core/app/src/ports.rs` з методами `insert_batch`, `finish_indexing`, `len`, `is_empty`, `clear`. Внутрішній стан `InMemoryIndex` обгорнуто в `RwLock`, що забезпечує потокобезпеку та одночасні операції читання під час заповнення індексу. Додано unit-тест `test_concurrent_batch_insertions` з 8 паралельними потоками записувачів та 1 фоновим потоком читача, який успішно верифікував відсутність втрат записів. Перевірено: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`. |
| T-017 Синхронізація пам'ять ↔ SQLite (запис дельт, відновлення при старті) | ✅ | 2026-07-08 | local | Додано `read_all_file_records` до порту `IndexStore` та `get_all` до порту `HotIndex`. Реалізовано ці методи в адаптері SQLite (`IndexDatabase`) та адаптері оперативної пам'яті (`InMemoryIndex`). Додано `trashradar-index-sqlite` як `dev-dependency` до `index-memory` для написання інтеграційного тесту `test_sqlite_to_in_memory_sync_and_checksum_verification`, який наповнює SQLite БД 1000 записами, завантажує їх в оперативну пам'ять (імітуючи відновлення при старті) та верифікує ідентичність даних через порівняння контрольних сум, розрахованих за примітивними полями через `DefaultHasher`. Перевірено: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`. |
| T-018 Пошук за іменем/шляхом серед записів індексу | ✅ | 2026-07-08 | local | Додано `search_file_records(query, limit)` до порту `HotIndex` і реалізацію `InMemoryIndex::search`: регістронезалежний підрядковий пошук, який іде по унікальних інтернованих директоріях/іменах (бітові маски збігів), а не по кожному з мільйонів записів; збіг через межу «директорія\\ім'я» перевіряється лише коли запит містить роздільник шляху. ASCII-швидкий шлях без алокацій + Unicode-фолбек. Результати обмежені кандидатами: записи з рішенням Keep не повертаються; є ліміт кількості результатів. 5 unit-тестів (ім'я/директорія/межа/ліміт/Keep) + ignored перф-гейт: пошук по 1 млн записів у release — 57 мс (рідкісний підрядок) і 55 мс (частий з лімітом 200) при цілі < 100 мс. Перевірено: `cargo fmt --check`; `cargo clippy --all-targets -- -D warnings`; `cargo test` (workspace, всі пройшли); `cargo test --release -p trashradar-index-memory search_one_million_records_under_100ms -- --ignored --nocapture` (1 passed). |
| T-019 Бенчмарк пам'яті та швидкості індексу | ✅ | 2026-07-08 | local | Створено standalone бенч-крейт `benches/index-bench` (окремо від `core/`, за repository.md §1) з CLI-гейтом на еталонному наборі 1 000 000 записів: фіксує пам'ять і швидкість `InMemoryIndex`, звіряє з закоміченим `baseline.json`, регрес >15% валить процес (exit 1). Метрика пам'яті детермінована (оцінка `capacity()` однакова на таргеті x86_64-pc-windows-msvc → байт-у-байт збіг baseline↔current) — жорсткий 15%-гейт, що реально валить CI. Тайминги (build/search rare/search frequent) машинозалежні на shared-runner'ах → регрес попереджає + абсолютна стеля; прапорець `--strict` робить їхній регрес жорстким локально; `--bless` перезаписує baseline. Baseline: memory 74.4 МБ, build ~2.1–2.6 с, search ~50 мс. Додано CI-джоб `bench` (fmt+clippy+run --release) і правило `.gitignore` для `benches/**/target/`. Верифіковано: check-режим exit 0; негативний тест (занижений memory baseline) → `1.43x FAIL (regress)`, exit 1; `cargo fmt --check` і `cargo clippy --all-targets -- -D warnings` по бенч-крейту чисті. |

| T-020 Спайк: читання MFT тому, вимір часу до повного переліку | ✅ | 2026-07-08 | local | Створено самодостатній FFI-спайк `spikes/mft-enum` (без зовнішніх крейтів), що перелічує всі файли тому через `FSCTL_ENUM_USN_DATA` і міряє час/пропускну здатність/пам'ять. Реальні заміри (elevated, SSD): C: 3.19 млн записів — холодний 50.5 с (~63k/с), теплий ~14–15 с (~213–225k/с); F: 230k за 3.2 с; D: 27k за 0.5 с. Пікова пам'ять переліку 4.7 МБ; проєкція індексу T-015 для 3.19 млн ≈ 213 МБ (< 300 МБ §15). **Вердикт: GO** для MFT-стратегії — теплий диск дає ~7 с на еталонні 1.5 млн файлів (ціль §15 < 10 с). Ключові знахідки для T-021: (1) `FSCTL_ENUM_USN_DATA` НЕ повертає розмір файла → потрібен сирий парсинг `$MFT`; (2) холодний прохід у 2–3× повільніший, вузьке місце — per-record обробка драйвера, не I/O (326 МБ за 50 с ≈ 6.5 МБ/с ≪ смуга SSD), сирий послідовний вичит `$MFT` очікувано швидший і дає розмір одразу. Повний звіт: docs/spikes/T-020-mft-enum.md. Перевірено: `cargo fmt --check` і `cargo clippy --all-targets -- -D warnings` по спайк-крейту чисті; тул коректно повертає ACCESS_DENIED без прав. |

| T-021 Промисловий MFT-парсер: метадані записів | ✅ | 2026-07-09 | local | Реалізовано сирий парсинг `$MFT` у `core/infra/scan-mft`, розділений на два шари. (1) `record.rs` — **чистий парсер байтів** запису FILE: фіксапи (Update Sequence Array з перевіркою торнутих записів), обхід атрибутів, `$STANDARD_INFORMATION` (дати+DOS-атрибути), `$FILE_NAME` (ім'я+батько, вибір найкращого простору імен, 8.3 не відкидається), unnamed `$DATA` (розмір), декодер run-list зі знаковими дельтами LCN. Продукує доменний `ScanEntry` (новий тип у `domain/src/scan.rs` — спільний вихід будь-якого `ScanSource`). 10 юніт-тестів на синтетичних записах (метадані/тека/фіксапи/торнутий/невживаний/розширювальний/сигнатура/пріоритет імені/run-list/varint) — рівень CI, без прав і I/O. (2) `volume.rs` (`#[cfg(windows)]`) — читач тому через WinAPI: `FSCTL_GET_NTFS_VOLUME_DATA` → run-list запису 0 (`$MFT`) → послідовний вичит усіх записів батчами по 8 МіБ у чистий парсер; локальний FFI (до `platform-win` T-028/T-034). Оголошено контракт порту `ScanSource::scan_volume` в app; `MftScanner` його реалізує. **DoD верифіковано** ігнорованим інтеграційним тестом на реальному томі (elevated): звірка з незалежним контролем FSCTL_ENUM_USN_DATA за номером запису MFT — на **тихому томі F: розбіжність 0** (230196 сирих проти 230179 USN, 0 пропущених, 0 різних імен; +17 сирих = системні метафайли, надмножина). На живому C: 2366 «пропущених» = жива зміна ФС за вікно скану, 14019 «різних імен» = гард-лінки WinSxS (той самий запис під різними іменами) — не помилки парсера, тому в тесті різні імена інформаційні, а покриття популяції — жорстке (на тихому томі). Перевірено: `cargo fmt --check`; `cargo clippy --workspace --exclude trashradar-shell --all-targets -- -D warnings`; `cargo test --workspace --exclude trashradar-shell` (усі пройшли); elevated `raw_mft_matches_usn_enumeration` на F: pass. |

| T-022 Побудова повних шляхів із записів MFT | ✅ | 2026-07-09 | local | Додано `paths.rs` у `scan-mft`: `PathResolver` відновлює повний шлях кожного `ScanEntry` через ланцюжок `parent_ref` до кореня (запис MFT №5), напр. `C:\Users\Ada\holiday.mp4`. Розкрутка **ітеративна** (не рекурсивна — глибокі шляхи не переповнюють стек), з мемоізацією шляхів директорій дорогою вниз, стелею глибини 1024 проти циклів у пошкодженій MFT і коректною обробкою розривів ланцюжка (`None` для сиріт/циклів). Hard links не роздувають розмір: парсер T-021 віддає один запис на фізичний файл, тож резолвер будує рівно один шлях на запис — агрегація рахує кожен раз (тест `hard_links_do_not_duplicate_size`). 8 юніт-тестів (ланцюжок/файл-у-корені/корінь/проміжна тека/сирота/цикл/мемоізація/hard-link) — рівень CI без I/O. **DoD верифіковано** ігнорованим елевованим тестом `resolved_paths_point_to_real_files` на тихому томі F:: вибірка 1052 файли — шлях побудовано для всіх, **0 відсутніх на диску, 100% існують, 0 розбіжностей розміру** (звірка з ground truth через `std::fs::metadata`). Перевірено: `cargo fmt --check`; `cargo clippy -p trashradar-scan-mft --all-targets -- -D warnings`; `cargo test --workspace --exclude trashradar-shell` (усі пройшли, scan-mft 18 passed + 2 ignored); elevated F: pass. |

| T-023 Класифікатор типів файлів за розширенням | ✅ | 2026-07-09 | local | Додано `FileKind::from_extension`/`from_path` у `domain/candidate.rs` — чиста класифікація без I/O, доступна сканеру й детекторам (T-041/042/043). Таблиця покриває Video/Image/Audio/Archive/Installer/DiskImage/Document (десятки розширень на клас, включно з raw-фото, контейнерами відео, образами дисків); невідоме, файли без розширення та dotfiles → `Other`. Код/конфіги свідомо `Other` (dev-артефакти — це структурні детектори T-049+, не розширення). 6 юніт-тестів (представники кожного класу / регістронезалежність / код→Other / повний шлях / multi-dot `tar.gz`→Archive / без розширення й dotfiles). **Вимір покриття на реальному томі F: (210 213 файлів, elevated):** розпізнано 58.7%, `Other` 41.3% — переважно файли без розширення (33.9k), код (js/dll/css/html/json/h/scss) і системне, що коректно лишається Other. Вимір виявив і закрито справжню прогалину: `.wmf`/`.emf` (векторні образи Windows) додано до Image. Критерій «≥95% файлів ЦА» валідується на еталонних машинах у беті (Фаза 1); інфо-тест `classifier_coverage_on_real_volume` дає інструмент виміру. Перевірено: `cargo fmt --check`; `cargo clippy --all-targets -- -D warnings`; `cargo test --workspace --exclude trashradar-shell` (усі пройшли). |

| T-024 Батчева видача результатів скану в індекс | ✅ | 2026-07-09 | local | Додано `pipeline.rs` у `scan-mft`, що зшиває скан→шляхи→класифікацію→індекс: чиста `to_file_record` (ScanEntry+шлях→FileRecord, тип за T-023) і `Batcher` — акумулятор, що здає `FileRecord` приймачу партіями по `batch_size`; синхронний приймач паузить продюсера — **зворотний тиск** тримає в пам'яті лише один батч. `#[cfg(windows)] scan_volume_to_index` робить два проходи MFT (перший збирає директорії для резолвера шляхів, другий стрімить файли батчами), пам'ять обмежена картою директорій + одним батчем. Модельне доповнення: додано `CategoryId::Uncategorized` (скановане, ще не заявлене детектором — architecture.md §3.4; живе лише в гарячому in-memory індексі, не входить у `ALL`, у SQLite не пишеться — CHECK-констрейнт це гарантує); оновлено розпакування категорії в index-memory та `category_name`/`parse_category` в index-sqlite (без зміни схеми/міграції). 5 юніт-тестів pipeline (конверсія / флаш на межі batch_size + остача / послідовні candidate_id / пропуск директорій і сиріт / помилка приймача зупиняє й пробрасується) — рівень CI без I/O. **DoD верифіковано** elevated на F:: 210 213 файлів у справжній `InMemoryIndex` через **22 батчі по 10k, макс. батч = 10000** (у льоті не більше одного батча), 0 пропущених; index.len == files_indexed, batches == ceil(files/N). Перевірено: `cargo fmt --check`; `cargo clippy --all-targets -- -D warnings`; `cargo test --workspace --exclude trashradar-shell` (усі пройшли, без регресів від Uncategorized). |

| T-025 Обробка помилок: недоступні/пошкоджені записи, зайняті томи | ✅ | 2026-07-09 | local | (зафіксовано постфактум) У `scan-mft`: `RecordError` (BAAD/torn/malformed) → запис пропускається з `tracing::debug`, скан триває; `ScanStats.corrupt` + підсумковий `warn`; некоректний/недоступний том → типізований `CoreError` (не паніка). Статистика пропусків пробрасується в `IndexingStats` (T-024). Покрито юніт-тестами парсера + `non_alphabetic_drive_is_invalid_argument`. |

| T-026 Паралельний обхід каталогів з work-stealing чергою | ✅ | 2026-07-09 | local | Реалізовано резервний шлях `ScanSource` у `core/infra/scan-walk`. `WalkScanner` + `walk_volume`/`walk_path`: пул воркерів (`available_parallelism` або явний `WalkConfig.workers`), локальний LIFO-стек + глобальна черга (половина нових підкаталогів іде на steal), `inflight`+condvar для коректного завершення. Кожен запис → доменний `ScanEntry` із синтетичними `file_ref`/`parent_ref` (корінь = 0), Win32-атрибути/FILETIME, reparse-точки не розгортаються (захист від циклів). Недоступні каталоги/записи — skip + `skipped_errors` (стиль T-025). `full_path` реконструює шлях за parent-ланцюжком. 6 CI-тестів (temp-дерево / parent-chain / 1-worker≡N-worker / workers_used / invalid drive / ScanSource) + ignored DoD `walk_population_matches_mft_on_volume` (звірка path+size з MFT, coverage ≥98%). Перевірено: `cargo fmt --check`; `cargo clippy -p trashradar-scan-walk --all-targets -- -D warnings`; `cargo test -p trashradar-scan-walk` (6 passed, 1 ignored); `cargo test/clippy --workspace --exclude trashradar-shell` без регресів. |

| T-027 Відсікання виключених шляхів на рівні обходу | ✅ | 2026-07-09 | local | У `scan-walk`: модуль `exclude.rs` — `PathExclusions` (нормалізація lower/`\`, префікс-сегмент, без хибного `WindowsBackup`⊇`Windows`); `windows_system_defaults(drive)` (Windows, Program Files, ProgramData, $Recycle.Bin, System Volume Information, Recovery, Config.Msi). `WalkConfig.exclusions` + `record_opened_dirs` (трейс). У `process_directory`: виключений child — **continue до metadata/read_dir**; виключений work-item — return до read_dir; виключений root — 0 syscall. `WalkStats.skipped_excluded`. `WalkScanner::with_system_exclusions`. **DoD:** тест `excluded_subtree_has_no_read_dir_and_no_entries` — secret/* відсутні в entries, жоден path у `opened_dirs` не під excluded; `excluded_root_skips_entire_walk`. 5 юніт-тестів PathExclusions. Перевірено: `cargo fmt --check`; `cargo clippy -p trashradar-scan-walk --all-targets -- -D warnings`; `cargo test -p trashradar-scan-walk` (13 passed, 1 ignored). |

| T-028 Автовибір шляху скану: MFT ↔ обхід за типом тому і правами | ✅ | 2026-07-09 | local | **App:** `choose_scan_strategy` (NTFS+elevated→Mft; інакше→DirectoryWalk) + `ScanStrategyReason` у domain; порт `ScanEnvironment`. **platform-win:** `WinScanEnvironment` — TokenElevation (advapi32), `GetVolumeInformationW`, `GetLogicalDrives`/`GetDriveTypeW`. **Shell health:** `elevated` + `scanPlans[]` (volume/strategy/reason/fileSystem); scanner module → online. **UI Health:** секція Scan strategy + elevation. **DoD:** unit-таблиця 4 кейси; `health_includes_scan_strategy_plans` — інваріант MFT лише при ntfs_elevated. Перевірено: `cargo test -p trashradar-app/platform-win/shell`; clippy `-D warnings`; `cargo fmt --check`. |

| T-029 Читання USN Journal, збереження позиції в індексі | ✅ | 2026-07-09 | local | **Domain:** `UsnCursor`/`UsnJournalInfo`/`UsnChange` + `usn_reason::*`. **App:** порт `ChangeSource` (`query_journal`/`read_delta` → `UsnReadOutcome::{Changes,JournalStale}`). **scan-usn:** парсер `USN_RECORD_V2` (CI); WinAPI QUERY/READ; `UsnChangeSource`; `capture_cursor_after_full_scan`. **index-sqlite v2:** `volume_usn_state` + get/set cursor; міграція з v1. **DoD:** cursor roundtrip у SQLite; unit-парсер дельти; ignored elevated `usn_delta_returns_only_new_changes` (create file → delta). Перевірено: `cargo test -p trashradar-scan-usn/index-sqlite`; clippy `-D warnings`; fmt. |

| T-030 Інкрементальне оновлення індексу за USN-дельтою | ✅ | 2026-07-09 | local | **App `usn_apply`:** `classify_usn_reason`, `FrnPathCache` (parent_ref+name→path), `plan_usn_mutations` → create/modify/delete/rename, `apply_mutations_to_hot_index`, `apply_usn_read_outcome` (дельта→hot index + SQLite delete + advance cursor; JournalStale без змін — T-031). **HotIndex:** `remove_paths`, `upsert_batch`. **IndexStore:** `delete_file_records_by_path`. **DoD unit:** create→modify size→delete; rename old→new; unresolved skip. Перевірено: `cargo test -p trashradar-app usn_apply` + index-memory/sqlite; clippy `-D warnings`. |

| T-031 Фолбек на повний скан при перезаписаному журналі | ✅ | 2026-07-09 | local | **Domain:** `FullRescanReason` + `user_message`. **App `usn_fallback`:** `process_usn_sync` → `UsnSyncResult::{Applied, FullRescanRequired}`; stale → `clear_usn_cursor` + reset `FrnPathCache` + `FullRescanRequest` (volume/reason/message). **IndexStore:** `clear_usn_cursor`. **Shell:** подія `scan.journal_stale` + `emit_journal_stale` / `JournalStaleEvent` (wiring старту скану — T-033). **UI types:** EventName + payload. **DoD unit:** journal_id_changed clears cursor + message; usn_below_lowest message; applied ≠ full rescan. Перевірено: usn_fallback + shell journal_stale tests; clippy `-D warnings`. |

| T-032 Change Monitor: живе оновлення індексу | ✅ | 2026-07-09 | local | **App `change_monitor`:** `monitor_tick` (USN delta→`process_usn_sync` per volume); `ChangeMonitor` background loop + `CancellationToken`; poll default 1s, cap 5s (DoD); `MonitorEvent::{Applied,FullRescanRequired,NoCursor,Error}`; `IndexUpdatedNotice`. **Shell:** `index.updated` event + `emit_index_updated`. **Contracts/UI:** EventName. **DoD unit:** delete applied in one tick; poll ≤5s; stale→full rescan; cancel stops thread. Lifecycle start у shell — T-033. Перевірено: `cargo test -p trashradar-app change_monitor` (6); clippy `-D warnings`. |

| T-033 IPC start/stop скану + progress | ✅ | 2026-07-09 | local | **App `scan_control`:** `ScanController`, `run_scan_session`, `CancellableVolumeScanner`, `SteppedTestScanner`; DoD cancel ≤500ms + partial batches. **scan-mft:** `enumerate_with_cancel` / `scan_volume_to_index_cancel`. **Shell `scan_runtime`:** `scan.start`/`scan.stop`, `ScanRuntime` (hot index + controller), MFT/walk auto strategy, `scan.progress` events. **Contracts/UI:** implemented. **DoD:** 4 unit scan_control + shell partial index on cancel. Перевірено: `cargo test -p trashradar-app scan_control`; `cargo test -p trashradar-shell` (18); clippy `-D warnings`. |

| T-034 Elevation: запит адмін-прав з поясненням, шлях відмови | ✅ | 2026-07-09 | local | **App `elevation`:** `ElevationSession` / `ElevationPromptKind` / `evaluate_elevation_prompt` — сесійна політика (offer → decline → no re-prompt); пояснення вигоди MFT українською. **platform-win:** `relaunch_elevated` через ShellExecuteExW `runas` (UAC cancel → `cancelled`). **Shell IPC:** `app.request_elevation` (relaunch + exit), `app.decline_elevation` (AtomicBool сесії), `app.health.elevation` {status, offerPending, message, ntfsVolumeCount}. **UI Health:** банер + кнопки «UAC» / «Продовжити без прав». **DoD:** з правами → MFT (T-028); після decline → walk + offer_pending=false у сесії. Перевірено: `cargo test -p trashradar-app elevation` (7); platform-win (6); shell elevation+повний (20); clippy `-D warnings`; `npm run build`. |

| T-035 Бенчмарк скану: 1 ТБ / 1.5 млн файлів | ✅ | 2026-07-09 | local | Standalone `benches/scan-bench` (як T-019): **synthetic CI** — PathResolver+Batcher→InMemoryIndex на **1 500 000** файлів; baseline fill ~3.3 с (~461k f/s), memory ~90.7 МБ; жорстка стеля **10 с (§15)** + memory hard-gate >15%; timings WARN+floor. **Live** `--volume X` (elevated): warmup+warm `scan_volume_to_index`, проєкція на 1.5 млн, fail якщо >10 с. CI job `bench` розширено (index-bench + scan-bench). Верифіковано: `--bless` + check exit 0; clippy `-D warnings`; fmt. |

| T-036 Контракт детектора | ✅ | 2026-07-09 | local | **Domain:** `Verdict::new` / `is_complete` (category+explanation+safety, §6.2). **App `detectors`:** trait `Detector` (id/category/is_enabled/evaluate), `DetectorId`, `DetectorHit`, `DetectorRegistry` (register/try_register, evaluate_record, evaluate_stream; disabled skip). DoD: тестові SizeProbe/PathProbe реєструються лише через `register` без змін «оркестратора»; вердикт з 3 полями. 5 unit + 1 domain. Перевірено: `cargo test -p trashradar-app detectors`; domain verdict; clippy `-D warnings`. |

| T-037 Оркестратор детекторів | ✅ | 2026-07-09 | local | **App `detectors/orchestrator`:** `DetectorOrchestrator` — `categorize_batch` (шлях під час скану → updated+hits), `categorize_index` (HotIndex get_all→батчі→upsert, cancel між батчами), `spawn_categorize_index` (WorkerPool T-008). Перший hit → FileRecord.category/safety/explanation/detector_id; усі hits для T-054; Keep skip; disabled = 0 evaluate. DoD: 7 unit (batch/disabled/index/keep/cancel/pool/multi-hit) + 5 T-036 = 12. clippy `-D warnings`. |

| T-038 Перерахунок порогів без рескану | ✅ | 2026-07-09 | local | **thresholds:** `ThresholdValue`, keys `min_size_bytes`/`min_age_days`. **Detector:** `set_threshold`/`get_threshold` (default no-op). **Registry:** set/get threshold by id. **Orchestrator:** `recalculate_batch`/`recalculate_index` (unmatched→Uncategorized), `set_threshold_and_recalculate`, `recalculate_inplace` (hot path). DoD: age 180→90 rebuilds OldFiles; raise threshold clears; **1M inplace ~126 мс < 1 с** (release ignored). IPC `category.set_threshold` лишається planned (shell farm wiring з T-039+). 16 unit (14+1 ignored). clippy `-D warnings`. |

| T-039 Детектор «Великі файли» | ✅ | 2026-07-09 | local | **`detectors/large_files`:** `LargeFilesDetector` (id `large_files`, CategoryId::LargeFiles); дефолт поріг **100 МіБ**; `evaluate` size≥порогу → Verdict explanation «розмір N ГБ» (`format_size_gb`), safety ReviewRecommended; лише `CandidateUnit::File`; `min_size_bytes` live (T-038). DoD: 7 unit. clippy `-D warnings`. |

| T-040 Детектор «Старі файли» | ✅ | 2026-07-09 | local | **`detectors/old_files`:** `OldFilesDetector` (id `old_files`); вік = accessed_at else modified_at ≥ `min_age_days` (дефолт **365**); пояснення «останній доступ/зміна N … тому»; `with_now_filetime` для тестів; live `min_age_days` (T-038). DoD: 9 unit + format age helpers. clippy `-D warnings`. |

| T-041 Детектор «Забуті відео» | ✅ | 2026-07-09 | local | **`detectors/forgotten_videos`:** `ForgottenVideosDetector` — Video ∧ size≥`min_size_bytes` (дефолт 100 МіБ) ∧ age≥`min_age_days` (дефолт 180); explanation «відео N ГБ, останній доступ …» (`forgotten_video_explanation`); обидва пороги live. DoD: 7 unit. clippy `-D warnings`. |

| T-042 Детектор «Архіви» | ✅ | 2026-07-09 | local | **`detectors/archives`:** `ArchivesDetector` — FileKind::Archive ∧ size≥`min_size_bytes` (дефолт **50 МіБ**); explanation «архів N ГБ»; zip/rar/7z/tar/gz + live поріг. DoD: 6 unit. clippy `-D warnings`. |

| T-043 Детектор «Інсталятори та ISO» | ✅ | 2026-07-09 | local | **`detectors/installers`:** `InstallersDetector` — DiskImage **будь-де**; Installer **лише** у Downloads/Download/Завантаження (`is_downloads_like_path`, сегментне порівняння); explanation «інсталятор…» / «образ диска…». DoD: 7 unit + path helper. clippy `-D warnings`. |

| T-044 Формат реєстру відомих локацій | ✅ | 2026-07-09 | local | **Схема v1** `registry/known-locations.json` + README. **App `location_registry`:** parse/validate (`LocationEntry`, `kind`, `safety`, `paths` з `%VAR%`), runtime `load_from_file`/`load_default` (TRASHRADAR_REGISTRY_DIR), `expand_path_template`, `path_matches_prefix`. DoD: новий JSON-запис без змін Rust; 10 unit. clippy `-D warnings`. |

| T-045 Temp-локації в реєстрі | ✅ | 2026-07-09 | local | **known-locations.json:** `windows.temp.user` (%TEMP%/%TMP%/Local\\Temp), `windows.temp.system` (WINDIR\\Temp), `windows.temp.programdata`, `windows.temp.internet` (INetCache); усі temp_files + safe_to_bulk. **API:** `expanded_roots`/`existing_roots`, `temp_locations`. DoD: user+system temp існують на Windows (2 unit). 12 location_registry tests. |

| T-046 Кеші топ-програм ЦА | ✅ | 2026-07-09 | local | **25 app_caches** у known-locations: Chrome/Edge/Firefox/Brave/Opera, VS Code/Cursor/JetBrains/VS, npm/yarn/pnpm/pip/cargo/NuGet, Discord/Telegram/Slack/Teams, Adobe Media/AE, Resolve, Blender, Steam, Spotify. `app_cache_locations()`. Live probe: 13 present / 12 absent на dev-машині. DoD catalog ≥20 + probe. |

| T-047 Детектор «Тимчасові файли» | ✅ | 2026-07-09 | local | **`detectors/temp_files`:** `TempFilesDetector::from_registry` — prefix-match на expanded `temp_files` roots; завжди `SafeToBulk` + `CategoryId::TempFiles`; rebuild_from_registry; app_caches ігноруються. DoD: 8 unit. clippy `-D warnings`. |

| T-048 Детектор «Кеші програм» | ✅ | 2026-07-09 | local | **`detectors/app_caches`:** `AppCachesDetector` — match `app_caches` roots; `evaluate` для файлів; **`aggregate_units`** → Folder-одиниці з Σ size + «Label · N ГБ · M файлів»; safety з реєстру. DoD: 7 unit. clippy `-D warnings`. |

## Легенда

✅ виконано й верифіковано · 🔄 в роботі · ⛔ заблоковано (причина в нотатках)

## Відхилення від документів (фіксувати обов'язково)

- T-002: repository.md §9 вимагає «+1 рев'ю» на PR — не вмикається, бо репозиторій
  однієї людини (власник не може заапрувити власний PR). Увімкнути при появі команди.
- T-019: DoD «регрес >15% валить CI» застосовано **жорстко лише до детермінованої
  метрики пам'яті** (`index_memory_bytes`). Тайминги на GitHub shared-runner'ах
  варіюються між прогонами й машинами, тож committed timing-baseline на них
  нестабільний — у CI їхній регрес попереджає (WARN) + ловиться абсолютною стелею.
  Жорсткий 15%-гейт таймингів доступний локально через `--strict` на машині
  baseline. Переглянути, якщо з'явиться dedicated bench-раннер зі стабільним залізом.
- T-002: **branch protection (required checks) НЕ застосовано** — GitHub Free не
  підтримує його на приватних репозиторіях (API: «Upgrade to GitHub Pro or make
  this repository public»). Готовий конфіг: contexts = UI (typecheck + build),
  Core (fmt + clippy + check), Core shell (tauri check); strict=false;
  enforce_admins=false. Застосувати одразу після переходу на public/Pro.
- T-035: CI-гейт `scan-bench` за замовчуванням міряє **синтетичний** конвеєр
  PathResolver+Batcher→index (1.5 млн) — без I/O MFT (shared-runner без
  elevation/репрезентативного тому). Жорстка стеля §15 10 с + memory hard-gate.
  Повний «теплий диск» — live `--volume X` (elevated, локально); проєкція на
  1.5 млн валить процес якщо >10 с. Узгоджено з політикою T-019 (CI = детерміноване
  + стеля; живий том — на машині розробника).
