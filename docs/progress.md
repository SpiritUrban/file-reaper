# TrashRadar — Execution Progress

> Журнал виконання задач з [tasks.md](tasks.md). Одна задача = один рядок.
> Правила ведення: статус міняється лише разом з фактом (коміт/PR/верифікація);
> нова сесія починає з першого рядка без ✅ у порядку tasks.md.

## Стан середовища розробки (локальна машина)

- Node 20.19.6, npm 10.8.2
- Rust 1.96.1 stable-msvc (rustup) + rustfmt + clippy; MSVC Build Tools 2022; WebView2 — є
- Tauri CLI 2.11.4 — через `ui` devDependencies (`npm run tauri`)
- Запуск повного застосунку: з `core/`: `..\ui\node_modules\.bin\tauri.cmd dev --config shell/tauri.conf.json`

## Журнал задач

| Задача | Статус | Дата | Де | Нотатки |
|---|---|---|---|---|
| T-001 Каркас (Tauri+Rust+React) | ✅ | 2026-07-08 | PR #1 | 13 крейтів + UI; `cargo check/clippy/fmt` чисто; UI build чисто; 4 екрани перевірені у браузері. Іконка-заглушка `core/shell/icons/icon.ico` (tauri-build вимагає .ico) — фінальна у T-159 |
| T-002 CI | ✅ | 2026-07-08 | PR #2 | 3 джоби (UI / Core / Core shell+rust-cache) — зелені; installer.yml збирає NSIS-артефакт на push у main + dispatch; локальна збірка інсталятора верифікована (TrashRadar_0.1.0_x64-setup.exe, 1.9 МБ). Попутно виправлено tauri.conf: before*Command виконуються з кореня workspace (../ui, не ../../ui). Branch protection НЕ застосовано — див. відхилення. Автовидалення гілок після merge увімкнено |
| T-003 Логування Core | ✅ | 2026-07-08 | PR #3 | tracing + tracing-subscriber (env-filter); власний RotatingWriter за розміром (5 МБ, 3 бекапи, 4 юніт-тести); файл %LOCALAPPDATA%\TrashRadar\logs\core.log; рівні через TRASHRADAR_LOG; паніки логуються; збій init → деградація до stderr. Живий запуск верифікував стартовий запис у файлі |
| T-004 Канал команд UI→Core | ✅ | 2026-07-08 | PR #5 | shell/src/ipc.rs: модуль команд; app.ping {delayMs?, fail?} — постійна діагностична команда (roundtrip + неблокування + конверт помилок E2E). 4 інтеграційні тести через tauri mock runtime (tauri/test dev-фіча): roundtrip, помилка = конверт T-007, паралельність (швидка <300мс поки повільна спить 400мс), deny_unknown_fields. UI: handshake ping при старті. Живий запуск: ping від реального UI у core.log. +tokio/time (вже в дереві) |
| T-005 Канал подій Core→UI | ✅ | 2026-07-08 | PR #6 | shell/src/events.rs: реєстр топіків + emit() (fire-and-forget, з логуванням). Дротові імена: Tauri забороняє крапки в подіях → app.test ↔ app:test (wire_name ↔ client.ts). app.test_stream: ack одразу, події у фоні (модель architecture.md §1.2). Capabilities: core/shell/capabilities/default.json (core:default) — без нього webview не має права listen. 3 тести шини (доставка, unlisten зупиняє, ack до завершення потоку). UI self-check обох каналів при старті. Живий запуск: 3 емісії в core.log |
| T-006 Тротлінг подій | ✅ | 2026-07-08 | local | Додано CounterThrottle у shell/src/events.rs: агрегує дельти лічильників у CounterSnapshot {delta,total}, тримає темп ≤10 оновлень/с і flush без втрати підсумку. Діагностичний app.test_stream тепер паралельно шле throttled app.test_counter; контракт і TS EventName синхронізовані. Перевірено: cargo fmt --check; CARGO_INCREMENTAL=0 cargo clippy -p trashradar-shell -- -D warnings; CARGO_INCREMENTAL=0 cargo test -p trashradar-shell (13 passed); npm run build |
| T-007 Модель помилок Core | ✅ | 2026-07-08 | PR #4 | domain::error::CoreError: конверт {code, message}, 5 стартових стабільних кодів (internal/invalid_argument/not_implemented/io/cancelled), From<io::Error>, 4 тести форми/roundtrip. UI: CoreErrorPayload у types.ts, CoreIpcError + нормалізація в client.ts (+ клієнтські коди ipc_unavailable/unknown). Конверт зафіксовано в contracts/ipc-contract.json → error_envelope. E2E через живий IPC — вправляється у T-004 |
| T-009 Діагностичний екран health | ✅ | 2026-07-08 | local | Додано UI-екран Health з live polling app.health: версія Core, статус, модулі shell/ipc/scanner/index/quarantine та IPC-лічильники commands/events/errors. Core накопичує метрики команд і подій, контракт app.health розширено для T-009, TS типи синхронізовані. Перевірено: cargo fmt --check; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo clippy -p trashradar-shell -- -D warnings; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo test -p trashradar-shell (13 passed); npm run build |
| T-008 Пул фонових воркерів | ✅ | 2026-07-08 | local | Додано WorkerPool у core/app: пріоритетна BinaryHeap-черга, FIFO у межах одного пріоритету, JobHandle з cancel/wait, CancellationToken для кооперативної відміни між кроками. Покрито тестами: пріоритетна задача виконується перед background у черзі; queued job скасовується до старту; running job бачить cancel між кроками. Перевірено: cargo fmt --check; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo clippy -p trashradar-app -- -D warnings; cargo test -p trashradar-app (3 passed); cargo clippy -p trashradar-shell -- -D warnings; cargo test -p trashradar-shell (13 passed) |
| T-010 SQLite WAL-обгортка | ✅ | 2026-07-08 | local | У core/infra/index-sqlite додано IndexDatabase: відкриття БД у профілі як index.sqlite3, створення директорії профілю, PRAGMA journal_mode=WAL/synchronous=NORMAL/foreign_keys=ON/busy_timeout, bootstrap metadata-таблиця та типізована помилка адаптера. Покрито тестами: БД створюється у профілі з WAL; окремий reader читає останній committed snapshot під час активної writer-транзакції. Перевірено: cargo fmt --check; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo test -p trashradar-index-sqlite (2 passed); CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo clippy -p trashradar-index-sqlite -- -D warnings |
| T-011 Схема БД v1 | ✅ | 2026-07-08 | local | У core/infra/index-sqlite додано documented migration v1 через SCHEMA_V1_SQL і PRAGMA user_version=1. Схема file_records покриває CandidateId, шлях, розмір, created/modified/accessed FILETIME, тип файлу, одиницю file/folder, категорію MVP, safety/decision, detector_id/explanation та FS-прапорці raw/read-only/hidden/system/temporary; додано індекси для майбутніх category-window запитів. Додано FileRecord/FileAttributes та upsert/read roundtrip. Перевірено: cargo fmt --check; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo test -p trashradar-index-sqlite (4 passed); CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo clippy -p trashradar-index-sqlite -- -D warnings |
| T-012 Механізм міграцій схеми | ✅ | 2026-07-08 | local | У core/infra/index-sqlite додано версійний runner міграцій з LATEST_SCHEMA_VERSION/MIGRATIONS, транзакційне виконання SQL-міграцій і typed SchemaMigrationFailed. Відкриття БД тепер автоматично мігрує стару T-010 БД до v1; якщо міграція старої схеми падає, адаптер закриває connection, видаляє index.sqlite3 та WAL/SHM sidecar-файли і відкривається з чистою схемою v1 без крашу. Покрито тестами: міграція T-010 meta БД зі збереженням даних; cold start після конфліктної старої таблиці file_records. Перевірено: cargo fmt --check; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo test -p trashradar-index-sqlite (6 passed); CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo clippy -p trashradar-index-sqlite -- -D warnings |
| T-013 Батчевий запис через єдину чергу писальника | ✅ | 2026-07-08 | local | У core/infra/index-sqlite додано upsert_file_records_batch через один BEGIN IMMEDIATE transaction і спільний prepared UPSERT для одиночного та батчевого запису. Додано IndexWriterQueue/IndexWriter: один background writer thread відкриває IndexDatabase, приймає батчі через mpsc-чергу та серіалізує всі записи в SQLite, щоб паралельні producer-и не створювали writer contention. PRAGMA cache_size/wal_autocheckpoint налаштовано для довгих батчів. Покрито тестами: батчевий upsert roundtrip; 8 конкурентних producer-ів через одну writer-чергу без database locked; ignored perf-gate на 1 000 000 записів батчами по 100k, окремо запущений і пройдений за ~64с при локальній цілі <=70с. Перевірено: cargo fmt --check; CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo test -p trashradar-index-sqlite (8 passed, 1 ignored); CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo test -p trashradar-index-sqlite writer_queue_inserts_one_million_records_in_batches -- --ignored --nocapture (1 passed); CARGO_TARGET_DIR=%TEMP%\trashradar-target-codex CARGO_INCREMENTAL=0 cargo clippy -p trashradar-index-sqlite -- -D warnings |

## Легенда

✅ виконано й верифіковано · 🔄 в роботі · ⛔ заблоковано (причина в нотатках)

## Відхилення від документів (фіксувати обов'язково)

- T-002: repository.md §9 вимагає «+1 рев'ю» на PR — не вмикається, бо репозиторій
  однієї людини (власник не може заапрувити власний PR). Увімкнути при появі команди.
- T-002: **branch protection (required checks) НЕ застосовано** — GitHub Free не
  підтримує його на приватних репозиторіях (API: «Upgrade to GitHub Pro or make
  this repository public»). Готовий конфіг: contexts = UI (typecheck + build),
  Core (fmt + clippy + check), Core shell (tauri check); strict=false;
  enforce_admins=false. Застосувати одразу після переходу на public/Pro.
