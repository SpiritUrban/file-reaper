# Scripts

| Файл | Призначення |
|---|---|
| `tauri.mjs` | Запуск Tauri з кореня (`npm run dev` / `npm run build`). Стартує з `core/`, щоб CLI знайшов `shell/tauri.conf.json`. |

Інше (інсталятор, бета-канал) — з T-159 / T-163.
