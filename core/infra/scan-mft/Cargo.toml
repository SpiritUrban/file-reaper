[package]
name = "trashradar-scan-mft"
description = "Адаптер ScanSource: читання MFT напряму (NTFS, швидкий шлях)."
version.workspace = true
edition.workspace = true

[dependencies]
trashradar-domain = { path = "../../domain" }
trashradar-app = { path = "../../app" }
tracing = { workspace = true }

[dev-dependencies]
# Для елевованого інтеграційного тесту T-024: скан у справжній HotIndex.
trashradar-index-memory = { path = "../index-memory" }
