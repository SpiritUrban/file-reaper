[package]
name = "trashradar-scan-walk"
description = "Адаптер ScanSource: паралельний обхід каталогів (резервний шлях)."
version.workspace = true
edition.workspace = true

[dependencies]
trashradar-domain = { path = "../../domain" }
trashradar-app = { path = "../../app" }
tracing = { workspace = true }

[dev-dependencies]
# DoD T-026: звірка популяції з MFT на реальному томі (лише тести).
trashradar-scan-mft = { path = "../scan-mft" }
